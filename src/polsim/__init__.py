"""polsim — political simulation and grand-strategy game (working title).

Milestone 2: geography and population — deterministic world generation
(provinces, electoral districts, towns), the struct-of-arrays citizen store
with exact population weights and correlated demographics, cached weighted
aggregates with incremental recalculation, and save schema v2 with
compressed per-district chunks and incremental saves. No gameplay yet; see
docs/ROADMAP.md.
"""

__version__ = "0.2.0"
